using System;
using System.Collections.Generic;
using System.Text;

namespace GamerSource.Models.Domain.PandaScore
{
    public class VideogameLeague : League
    {
        //This will be filled out once other games are supported via the League model but for now it inherits all of its requirements.

          //"id",
          //"image_url",
          //"live_supported",
          //"modified_at",
          //"name",
          //"series",
          //"slug",
          //"url"

    }
}

